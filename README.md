# 🌌 **AstrumAtlas — Astronomic Objects & Events Directory**

**AstrumAtlas** (*astrum — звезда, atlas — сборник*) — это веб-платформа для просмотра астрономических объектов, изучения космических событий и ведения личного списка «избранного».

## 🚀 **Стек технологий**

### 🎨 **Frontend**
- React 18 + Vite
- React Router
- Tailwind CSS (космическая тема)
- Axios
- Lucide React (иконки)
- React Hook Form

### 🐍 **Backend**
- FastAPI
- SQLAlchemy + Alembic
- PostgreSQL
- JWT Authentication
- Pydantic

## 🛠️ **Быстрый старт**

### Backend
```bash
cd backend
pip install -r requirements.txt
cp .env.example .env
# Настройте DATABASE_URL в .env
uvicorn app.main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

### Docker (рекомендуется)
```bash
cd backend
docker-compose up -d
```

## 📱 **Особенности UI**

### 🎨 **Дизайн**
- **Космическая тема**: Темные цвета с градиентами space/cosmic
- **Стеклянные эффекты**: Backdrop blur для карточек
- **Адаптивность**: Mobile-first подход
- **Анимации**: Hover эффекты и плавные переходы

### 🧩 **Компоненты**
- `ObjectCard` - Карточки объектов с избранным
- `EventCard` - Карточки событий с датами
- `Header` - Навигация с градиентным логотипом
- `LoadingSpinner` - Космический спиннер

### 🎯 **Страницы**
- **Главная** - Hero секция + статистика
- **Объекты** - Каталог с фильтрами и поиском
- **События** - Календарь с фильтрами по дате/типу
- **Избранное** - Личная коллекция пользователя
- **Авторизация** - Современные формы входа/регистрации

## 🔗 **API Endpoints**

### Auth
- `POST /auth/register` - Регистрация
- `POST /auth/login` - JWT авторизация
- `GET /auth/me` - Текущий пользователь

### Objects
- `GET /objects/` - Список с фильтрами
- `GET /objects/{id}` - Детали объекта
- `POST /objects/` - Создать (admin)

### Events
- `GET /events/` - Список событий
- `GET /events/{id}` - Детали события

### Favorites
- `GET /favorites/` - Избранные пользователя
- `POST /favorites/{object_id}` - Добавить
- `DELETE /favorites/{object_id}` - Удалить

## 🗄️ **База данных**

```sql
users (id, username, email, password_hash, is_admin)
objects (id, name, type, distance_ly, magnitude, constellation_id)
events (id, title, date, type, visibility)
constellations (id, name, short_description)
favorites (user_id, object_id)
```

## 🎨 **Кастомизация дизайна**

### Цветовая палитра
```css
space: #0c0c0c → #312e81 (темные тона)
cosmic: #fdf4ff → #701a75 (акценты)
```

### Градиенты
```css
space-gradient: linear-gradient(135deg, #0c0c0c 0%, #1a1a2e 50%, #16213e 100%)
cosmic-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%)
```

### Компоненты
```css
.card - стеклянный эффект с backdrop-blur
.btn-primary - космический градиент
.input-field - темные поля с cosmic акцентами
```

## 📊 **Готовые фичи**

✅ JWT аутентификация  
✅ CRUD для объектов/событий  
✅ Система избранного  
✅ Фильтры и поиск  
✅ Адаптивный дизайн  
✅ Космическая тема  
✅ API документация (/docs)  
✅ Docker setup  

## 🚀 **Для дальнейшего развития**

- Детальные страницы объектов
- Интеграция с NASA API
- Карта звездного неба
- Уведомления о событиях
- Социальные функции
- Темная/светлая тема
- Мобильное приложение

---

**Проект готов к запуску и дальнейшей кастомизации!** 🌟